# ============================================
# 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env

# ============================================
# 必需配置
# ============================================
# Anthropic API 密钥（必需）
ANTHROPIC_API_KEY=your-api-key-here
ANTHROPIC_BASE_URL=https://api.anthropic.com

# ============================================
# 服务器配置
# ============================================
# 前端端口（默认 3000）
FRONTEND_PORT=8888

# 后端端口（默认 8000）
BACKEND_PORT=8000

# 调试模式（默认 false）
DEBUG=false

# ============================================
# Agent 配置
# ============================================
# 默认模型（可选：sonnet, opus, haiku）
DEFAULT_MODEL=sonnet

# 最大对话轮数
MAX_TURNS=20

# 权限模式（可选：acceptEdits, autoConfirm）
PERMISSION_MODE=acceptEdits

# 允许的工具列表（逗号分隔）
DEFAULT_ALLOWED_TOOLS=Read,Write,Edit,Bash,Glob,Grep,WebSearch,WebFetch,TodoWrite

# ============================================
# 数据库配置（生产环境可选）
# ============================================
# SQLite（默认）
# DATABASE_URL=sqlite:///./data/aigc.db

# PostgreSQL（推荐用于生产）
# DATABASE_URL=postgresql://user:password@localhost:5432/aigc

# MySQL
# DATABASE_URL=mysql://user:password@localhost:3306/aigc

# ============================================
# 安全配置（可选）
# ============================================
# CORS 允许的源（逗号分隔）
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# JWT 密钥（用于用户认证）
JWT_SECRET_KEY=your-secret-key-change-in-production

# 访问令牌过期时间（天）
ACCESS_TOKEN_EXPIRE_DAYS=30

# ============================================
# MinIO 配置（可选）
# ============================================
# MINIO_ENDPOINT=http://localhost:9000
# MINIO_ACCESS_KEY=your-minio-access-key
# MINIO_SECRET_KEY=your-minio-secret-key
# MINIO_BUCKET_NAME=agentic
# MINIO_REGION=us-east-1
# MINIO_SECURE=false

# ============================================
# Tavily Search API（可选）
# ============================================
# TAVILY_API_KEY=your-tavily-api-key-here

# ============================================
# 日志配置（可选）
# ============================================
# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# 日志文件路径
# LOG_FILE=/app/logs/app.log

# ============================================
# 代理配置（可选）
# ============================================
# HTTP 代理
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS 代理
# HTTPS_PROXY=http://proxy.example.com:8080

# 不使用代理的地址
# NO_PROXY=localhost,127.0.0.1
