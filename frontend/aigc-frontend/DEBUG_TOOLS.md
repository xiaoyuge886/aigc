# 工具调用历史功能调试指南

## 🐛 调试步骤

### 1. 打开浏览器控制台
```
Chrome/Edge: F12 或 Cmd+Option+I (Mac) / Ctrl+Shift+I (Windows)
Firefox: F12 或 Cmd+Option+I (Mac) / Ctrl+Shift+I (Windows)
```

### 2. 查看初始化日志
页面加载时应该在控制台看到：
```
🔧 Mock 工具调用数据已加载: 3 个
🔧 工具调用状态更新: { total: 3, calls: [...] }
```

### 3. 切换到"工具"标签
点击工作区的"工具"标签，应该看到：
```
🔧 工具标签页已激活 { totalCalls: 3, selectedCall: null, calls: [...] }
```

### 4. 验证数据结构
检查 `calls` 数组，每个工具调用应该包含：
- `id`: 工具调用 ID (例如: `call_8e4a8300...`)
- `name`: 工具名称 (例如: `WebSearch`)
- `status`: 状态 (例如: `success`)
- `timestamp`: 时间戳 (例如: `14:30:25`)

## 📋 Mock 数据内容

### 工具 1: AI Agents 趋势搜索
```
ID: call_8e4a830039814e8fb06f0ccf
名称: WebSearch
输入: "AI agents robotics multimodal AI 2025 trends"
输出: 包含 McKinsey、Medium 等来源的搜索结果
时间: 14:30:25
耗时: 1250ms
```

### 工具 2: 中文 AI 趋势搜索
```
ID: call_f6ae5ea89c7e466ab9492d9f
名称: WebSearch
输入: "人工智能 AI 技术趋势 2025 最新发展"
输出: 包含德勤、Gartner 等来源的搜索结果
时间: 14:30:28
耗时: 1180ms
```

### 工具 3: LLM 模型对比
```
ID: call_27ed91628ca7441f97af3fc7
名称: WebSearch
输入: "AI technology trends 2025 LLM GPT Claude Gemini"
输出: 包含 Gemini 3、Claude 4.5、GPT-5.1 等模型对比
时间: 14:30:31
耗时: 1320ms
```

## ✅ 预期界面效果

### 列表视图
```
┌─────────────────────────────────────────────────────┐
│ 🔧 Tool Calls                        3 Total Calls  │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 🔁 WebSearch                        ✓ success       │
│    call_8e4a830039814e8fb06f0ccf                     │
│    14:30:25  1250ms                                   │
│                                         →            │
│                                                      │
│ 🔁 WebSearch                        ✓ success       │
│    call_f6ae5ea89c7e466ab9492d9f                     │
│    14:30:28  1180ms                                   │
│                                         →            │
│                                                      │
│ 🔁 WebSearch                        ✓ success       │
│    call_27ed91628ca7441f97af3fc7                     │
│    14:30:31  1320ms                                   │
│                                         →            │
└─────────────────────────────────────────────────────┘
```

### 详情视图（点击列表项后）
```
┌─────────────────────────────────────────────────────┐
│ ← 返回列表                                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 🔧 WebSearch                         ✓ success     │
│    call_8e4a830039814e8fb06f0ccf                     │
│                                            14:30:25  │
│                                            1250ms    │
│                                                      │
│ ┌───────────────────────────────────────────────┐  │
│ │ INPUT                                         │  │
│ │ ┌─────────────────────────────────────────┐  │  │
│ │ │ {                                       │  │  │
│ │ │   "query": "AI agents robotics..."      │  │  │
│ │ │ }                                       │  │  │
│ │ └─────────────────────────────────────────┘  │  │
│ └───────────────────────────────────────────────┘  │
│                                                      │
│ ┌───────────────────────────────────────────────┐  │
│ │ OUTPUT                                        │  │
│ │ Web search results for query: "..."          │  │
│ │                                               │  │
│ │ Links: [...]                                  │  │
│ │                                               │  │
│ │ # Search Results...                          │  │
│ │ ## Key 2025 AI Trends                        │  │
│ │ ...                                           │  │
│ └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## 🔍 常见问题排查

### 问题 1: 工具标签不显示任何内容
**检查：**
- 控制台是否有 `🔧 工具调用状态更新` 日志？
- `total` 数量是否为 3？
- 是否有任何 JavaScript 错误？

**解决：**
- 检查 `toolCalls` 状态是否正确初始化
- 检查 `activeTab === 'tools'` 条件是否满足

### 问题 2: 列表显示但点击无反应
**检查：**
- 控制台是否有 `selectedCall` 变化日志？
- 点击事件是否正确绑定？

**解决：**
- 检查 `setSelectedToolCall` 是否被调用
- 检查条件渲染逻辑

### 问题 3: 详情页面显示不正确
**检查：**
- `tool.tool_name` 是否正确显示？
- `tool.input` 和 `tool.output` 是否有数据？

**解决：**
- 检查 mock 数据结构
- 检查数据格式化逻辑

## 📝 调试日志位置

### 组件初始化
```typescript
// ChatInterface.tsx:1008
console.log('%c🔧 Mock 工具调用数据已加载:', ...);
```

### 状态更新
```typescript
// ChatInterface.tsx:990
console.log('%c🔧 工具调用状态更新:', ...);
```

### 标签页激活
```typescript
// ChatInterface.tsx:1541
console.log('🔧 工具标签页已激活', ...);
```

## 🚀 下一步调试

1. **验证 mock 数据加载** - 刷新页面，检查控制台初始化日志
2. **验证标签页渲染** - 点击"工具"标签，检查是否有渲染日志
3. **验证列表显示** - 查看是否有 3 个工具调用列表项
4. **验证点击事件** - 点击列表项，检查是否显示详情
5. **验证详情内容** - 检查输入和输出是否正确格式化显示

## 💡 实际数据集成

当 mock 数据测试通过后，可以：
1. 移除 mock 数据初始化
2. 使用 `parseToolCalls()` 函数解析实际响应
3. 在流式响应完成后自动添加到状态

```typescript
// 在 handleSend 的响应完成后
const parsedToolCalls = parseToolCalls(full);
if (parsedToolCalls.length > 0) {
  setToolCalls(prev => [...prev, ...parsedToolCalls]);
}
```
