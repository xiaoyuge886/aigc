
 # **Agent OS PRD & 总体架构说明（V1.1）**


## 一、产品背景与问题定义（Why）

### 1. 业务背景

在大量真实业务场景（OpenWork）中，工作具有以下特征：

* 目标模糊、需澄清
* 过程复杂、不可一次性规划
* 强依赖文档、系统、规则
* 需要人与系统反复协作
* 结果不是一句话，而是「工件」

传统工具的问题是：

* 自动化只能做“确定流程”
* AI 助手只能“回答问题”
* 业务无法信任黑盒执行

### 2. 核心问题

如何让 AI 像一个“可控、可解释、能协作的数字同事”，完成一次真实业务工作？

---

## 二、产品目标（Objective）

构建一个面向业务侧的 **Agent OS**，基于 Claude Agent SDK 的 **Skill 机制**，在一次 Agent Run 中：

* 理解业务目标
* 拆解执行计划
* 以 Skill 为单位执行工作
* 在关键节点与人协作
* 将全过程沉淀为记忆（mem0）
* 并以可视化方式完整呈现

### 成功标准（Success Criteria）

* ✅ 一次 Agent Run 工程完整
* ✅ 过程可解释、可回放
* ✅ 业务可介入、可纠错
* ✅ 能力可扩展、可复用
* ✅ 支持 Skill 在线生成 / 调试 / 注册与 Marketplace

---

## 三、目标用户与使用场景（Who & Where）

### 目标用户

* 业务人员 / 专家
* 不具备 AI / 编程背景
* 关注「是否把事办成」

### 典型场景

* 复杂文档分析与抽取
* 规则/政策/制度整理
* 跨系统信息整合
* 专题研究与汇总
* 标准化报告生成
* Skill 快速构建与复用

---

## 四、核心设计原则（Design Principles）

1. Skill 是业务级能力单元
2. Agent ≠ 模型
3. 记忆是系统资产（mem0）
4. 多 Agent 是默认
5. 执行必须受控（Sandbox）
6. 行为必须被看见（A2UI）
7. 业务不感知 MCP / Tool
8. Skill Online & Marketplace 支持动态扩展

---

## 五、核心功能需求（Functional Requirements）

### **FR-1 目标理解与澄清**

* 输入自然语言业务目标
* Agent 输出结构化目标理解
* 支持业务确认 / 修改
* 明确约束与成功条件

### **FR-2 规划（Planning）**

* Agent 自动生成 Skill 级计划
* 支持 DAG / 顺序 / 并行
* 计划可动态调整
* 支持中途暂停 / 继续

### **FR-3 Skill 驱动执行**

* Agent 基于计划选择 Skill
* Skill 定义清晰输入 / 输出
* Skill 内部可使用 MCP / API / Sandbox
* 支持失败处理、重试、回退
* **支持 Skill 在线生成 / 调试 / 注册 / Marketplace**

### **FR-4 人在回路（HITL）**

* Agent 在关键节点主动请求确认
* 人的决策影响后续执行
* 人工修改结果可写回记忆

### **FR-5 状态管理与可解释性**

* Agent 明确状态机（planning / acting / waiting / done）
* 每一步行为有可追溯记录
* 可回答「在做什么 / 为什么」

### **FR-6 记忆系统（mem0）**

* Episodic Memory：一次 Run 全过程
* Long-term Memory：跨任务业务知识
* Procedural Memory：Skill 使用经验
* 记忆参与 Planning / Skill 选择 / Validation

### **FR-7 多 Agent 协作（A2A）**

* 不同职责 Agent 分工
* Agent 之间事件驱动协作
* 共享 mem0，但职责隔离

### **FR-8 展现层（A2UI）**

* 展示内容：目标理解 / 执行计划 / Skill 使用轨迹 / Agent 状态 / 结果工件 / 记忆回放
* UI 不承载智能逻辑
* 支持双向映射（UI 事件 → Sub-Intent）

### **FR-9 安全与可控**

* Skill 在 Sandbox 运行
* Planner 执行受控
* 全链路审计与权限管理

### **FR-10 扩展性 / Skill Marketplace**

* **Skill Online Builder**：可视化或 DSL 编辑 Skill
* **在线调试**：在 Sandbox 中实时执行 Skill，支持 HitL 模拟
* **注册与版本管理**：Skill 可热加载，支持版本控制、依赖管理
* **内部 / 企业 Marketplace**：搜索、标签、评分、分享与权限控制
* **动态调用**：Planner 可即时调用新 Skill，无需重新部署

---

## 六、非功能性需求（NFR）

* 可扩展性：新增 Skill / Agent 不影响现有
* 可维护性：Run 可回放、可 Debug
* 安全性：执行在 Sandbox 中
* 可观测性：事件、状态、日志统一
* 可替换性：模型可替换，不锁死

---

## 七、系统总体架构（Architecture）

### 1️⃣ 总体分层架构

```
┌──────────────────────────────────────────────┐
│             业务展现层（A2UI）               │
│  - 目标 / 计划 / 进度                       │
│  - Skill 轨迹                               │
│  - Agent 状态                               │
│  - 结果工件 / 记忆回放                      │
└────────────────▲─────────────────────────────┘
                 │ A2UI Events
┌────────────────┴─────────────────────────────┐
│        Agent 协调 / 事件层                    │
│  - Agent State Machine                       │
│  - Run / Task / Step                         │
│  - A2A 协作                                 │
└────────────────▲─────────────────────────────┘
                 │
┌────────────────┴─────────────────────────────┐
│        Agent Runtime（Claude SDK）            │
│  - Reasoning                                 │
│  - Planning                                  │
│  - Skill Selection & Invocation              │
│  - Human-in-the-loop                         │
│  - Skill Online / Marketplace Builder        │
│                                              │
│   read / write                               │
│        ↓                                     │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│             🧠 mem0（记忆中枢）               │
│  - Episodic / Long-term / Procedural         │
│  - 检索 / 写入 / 组织                        │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│          Skill Runtime（核心抽象）            │
│  - Skill Schema                              │
│  - 输入 / 输出                               │
│  - Sandbox / 失败模式 / 重试                 │
│  - 在线生成 / 调试 / 热加载 / Marketplace      │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│        底层能力（业务不可见）                │
│  - MCP Servers                               │
│  - DB / File / External API                  │
│  - Sandbox                                   │
└──────────────────────────────────────────────┘
```

---

### 2️⃣ 一次 Agent Run 的时序（简化）

```
业务目标
  ↓
目标理解 → 人确认
  ↓
Skill 级规划
  ↓
Skill 执行（多步，可调用在线 Skill / Marketplace）
  ↓
必要时请求人（HITL）
  ↓
结果生成
  ↓
写入 mem0
  ↓
A2UI 回放
```

---

## 八、关键抽象定义（供开发使用）

* **Agent Run**：run_id / goal / plan(skills[]) / state / result / memory_refs
* **Skill**：skill_id / description / input_schema / output_schema / preconditions / failure_modes / version
* **Memory（mem0）**：content / metadata(run_id / agent / skill_id / memory_type)

---

## 九、阶段性里程碑（MVP）

**V1 交付标准**

* 单一业务场景
* 单 Run 完整闭环
* 3–5 核心 Skill
* mem0 记忆写入与回放
* A2UI 全流程展示
* **支持 Skill 在线生成 / 调试 / Marketplace**

---

## 十、一句话对外表述（最终版）

> Agent OS 是一个面向业务的智能工作系统，
> 它以 Skill 为核心执行单元，
> 通过 Agent 自主规划与执行，
> 在人类协作与长期记忆支持下，
> 完整、可信地完成一次真实工作，
> 并允许 Skill 在线生成、调试与市场化复用。

---

✅ **V1.1 最大亮点更新**

* Skill Online Builder + Runtime 调试
* Skill Marketplace + 热加载
* 双向 UI → Intent 映射
* 完整闭环的状态管理 + 自我修复机制
* 多 Agent 协作 + Mem0 支持长期经验沉淀

