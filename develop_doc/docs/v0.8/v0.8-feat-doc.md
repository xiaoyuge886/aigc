# 技能市场架构设计 V0.8

> 让用户可以创建、调试和发布 AI 技能，构建开放的技能生态系统
>
> 最后更新：2026-01-14

---

## 🎯 核心设计原则

### 原则0: 不要改变现有问答逻辑
如果需要修改，需要和用户确认

### 原则1：充分复用现有能力
- **复用对话系统**：使用现有的多轮对话机制实现技能调试
- **复用技能系统**：基于现有的技能加载和管理机制
- **复用数据库模型**：使用现有的 SkillDB 模型，无需大规模重构

### 原则2：对话式构建体验
- **自然交互**：通过对话引导用户创建技能，而非复杂表单
- **实时调试**：在创建过程中即可测试技能效果
- **所见即所得**：立即看到技能生成结果

### 原则3：渐进式发布
- **草稿阶段**：创建技能但仅自己可见
- **调试阶段**：在对话中测试和优化
- **发布阶段**：发布到市场，所有人可用

### 原则4：开放生态
- **技能模板**：提供常见技能模板，降低创建门槛
- **技能评分**：用户可为技能评分和反馈
- **推荐算法**：基于使用统计推荐热门技能

---

## 🏗️ 分层架构设计

### 架构层次

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 0: 现有基础层（Existing Foundation Layer）             │
│  - 技能数据库模型（SkillDB）                                  │
│  - 技能管理API（CRUD）                                       │
│  - 多轮对话系统（AgentSession）                               │
│  - 技能加载器（SkillLoader/SkillManager）                     │
│  特点：已实现，无需修改                                        │
└─────────────────────────────────────────────────────────────┘
                           ↓ (复用)
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: 技能构建层（Skill Builder Layer）- 核心            │
│  - Meta Skill（skill_builder）                               │
│  - 对话式引导流程                                             │
│  - 技能生成逻辑                                               │
│  - SKILL.md 和 skill.json 生成                               │
│  特点：通过对话创建技能                                       │
└─────────────────────────────────────────────────────────────┘
                           ↓ (复用)
┌─────────────────────────────────────────────────────────────┐
│  Layer 2: 调试层（Debug Layer）- 核心                        │
│  - 调试会话管理                                               │
│  - 技能临时加载                                               │
│  - 多轮测试                                                   │
│  - 实时预览                                                   │
│  特点：在对话中实时测试技能                                   │
└─────────────────────────────────────────────────────────────┘
                           ↓ (新增)
┌─────────────────────────────────────────────────────────────┐
│  Layer 3: 市场层（Market Layer）- 扩展                       │
│  - 技能市场页面                                               │
│  - 分类筛选和搜索                                             │
│  - 技能详情展示                                               │
│  - 评分和评论系统                                             │
│  特点：展示和发现技能                                         │
└─────────────────────────────────────────────────────────────┘
                           ↓ (新增)
┌─────────────────────────────────────────────────────────────┐
│  Layer 4: 生态层（Ecosystem Layer）- 可选                    │
│  - 技能模板库                                                 │
│  - 推荐算法                                                   │
│  - 使用统计和分析                                             │
│  - 审核和举报机制                                             │
│  特点：增强生态体验                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 需求明确

### 需求1：技能构建功能（必须）

#### 1.1 Meta Skill（技能构建器）

**核心思想**：创建一个特殊的技能 `skill_builder`，它本身是一个技能，用于创建其他技能。

**特点**：
- ✅ 通过对话引导用户创建技能
- ✅ 自动生成 SKILL.md 和 skill.json
- ✅ 支持技能模板
- ✅ 复用现有对话系统

**实现方式**：
```python
# backend/.claude/skills/skill_builder/SKILL.md

---
name: skill_builder
description: 技能构建助手，帮助用户通过对话创建自定义技能
---

# Skill Builder - 技能构建助手

你是技能构建专家，帮助用户创建新的 AI 技能。

## 工作流程

### 阶段1：需求收集
通过多轮对话了解用户想要创建的技能：
- 技能名称
- 主要功能
- 使用场景
- 特殊要求

### 阶段2：技能设计
基于用户需求，设计技能结构：
- 生成 skill.json 配置
- 生成 SKILL.md 内容大纲
- 确定能力边界

### 阶段3：内容生成
生成完整的技能内容：
- 编写详细的 SKILL.md
- 配置 skill.json 参数
- 添加使用示例

### 阶段4：测试验证
在对话中测试技能：
- 提供测试用例
- 模拟调用场景
- 根据反馈优化

### 阶段5：发布准备
确认并发布：
- 技能名称和描述
- 分类标签
- 是否公开
- 确认发布
```

#### 1.2 技能生成API

**核心功能**：
```python
# backend/services/skill_builder_service.py

class SkillBuilderService:
    """技能构建服务"""

    async def build_skill_from_conversation(
        self,
        conversation_context: Dict,
        user_id: int
    ) -> SkillDB:
        """
        从对话上下文构建技能

        Args:
            conversation_context: 对话上下文（包含需求信息）
            user_id: 创建者ID

        Returns:
            SkillDB: 创建的技能对象
        """
        # 1. 提取技能元数据
        skill_meta = self._extract_metadata(conversation_context)

        # 2. 生成 skill.json
        skill_config = self._generate_skill_config(skill_meta)

        # 3. 生成 SKILL.md
        skill_content = self._generate_skill_content(skill_meta)

        # 4. 保存到数据库
        skill = SkillDB(
            name=skill_meta['name'],
            description=skill_meta['description'],
            category=skill_meta['category'],
            skill_content=skill_content,
            skill_config=skill_config,
            created_by=user_id,
            is_public=False  # 初始为草稿状态
        )

        return await self._save_skill(skill)
```

---

### 需求2：对话调试功能（必须）

#### 2.1 调试会话管理

**核心思想**：复用现有的会话机制，创建临时调试会话。

**实现方式**：
```python
# backend/services/skill_debugger.py

class SkillDebugger:
    """技能调试器"""

    async def start_debug_session(
        self,
        skill_id: int,
        user_id: int
    ) -> str:
        """
        启动调试会话

        Args:
            skill_id: 要调试的技能ID
            user_id: 用户ID

        Returns:
            str: 调试会话ID
        """
        # 1. 创建专用调试会话
        debug_session_id = f"debug_skill_{skill_id}_{user_id}_{int(time.time())}"

        # 2. 加载要调试的技能
        skill = await self._load_skill(skill_id)

        # 3. 将技能注入到会话的 system prompt 中
        await self._inject_skill_to_session(
            debug_session_id,
            skill
        )

        # 4. 返回会话ID供前端连接
        return debug_session_id

    async def test_skill(
        self,
        debug_session_id: str,
        test_input: str
    ) -> AsyncIterator:
        """
        在调试会话中测试技能

        Args:
            debug_session_id: 调试会话ID
            test_input: 测试输入

        Yields:
            流式响应
        """
        # 使用现有的 query_in_session 机制
        async for response in self.agent_service.query_in_session(
            prompt=test_input,
            session_id=debug_session_id,
            include_partial_messages=True
        ):
            yield response
```

#### 2.2 前端调试界面

```typescript
// frontend/components/SkillDebugger.tsx

interface SkillDebuggerProps {
  skillId: number;
}

export function SkillDebugger({ skillId }: SkillDebuggerProps) {
  const [messages, setMessages] = useState<Message[]>([]);
  const [debugSessionId, setDebugSessionId] = useState<string>("");

  // 启动调试会话
  useEffect(() => {
    async function initDebugSession() {
      const response = await fetch(`/api/v1/platform/skills/${skillId}/debug`, {
        method: 'POST'
      });
      const { session_id } = await response.json();
      setDebugSessionId(session_id);
    }
    initDebugSession();
  }, [skillId]);

  // 发送测试消息
  const sendTestMessage = async (prompt: string) => {
    const response = await fetch(`/api/v1/session/query/stream`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        prompt,
        session_id: debugSessionId
      })
    });

    // 处理流式响应...
  };

  return (
    <div className="skill-debugger">
      <div className="debug-panel">
        <h3>技能调试面板</h3>
        <ChatInterface
          sessionId={debugSessionId}
          onMessage={sendTestMessage}
        />
      </div>
      <div className="skill-preview">
        <h4>技能内容预览</h4>
        {/* 显示技能的 SKILL.md 和 skill.json */}
      </div>
    </div>
  );
}
```

---

### 需求3：技能市场功能（重要）

#### 3.1 市场页面设计

**核心功能**：
- 技能列表展示
- 分类筛选
- 搜索功能
- 技能评分
- 使用统计

#### 3.2 技能详情页面

**展示内容**：
- 技能完整信息
- SKILL.md 内容预览
- 使用示例
- 评分和评论
- 启用/试用按钮

#### 3.3 评分和统计系统

```python
# backend/models/database.py（扩展）

class SkillRatingDB(Base):
    """技能评分表"""
    __tablename__ = "skill_ratings"

    id: Mapped[int] = mapped_column(primary_key=True)
    skill_id: Mapped[int] = mapped_column(Integer, ForeignKey("skills.id"))
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"))
    rating: Mapped[int] = mapped_column(Integer)  # 1-5星
    comment: Mapped[Optional[str]] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

class SkillUsageStatsDB(Base):
    """技能使用统计表"""
    __tablename__ = "skill_usage_stats"

    skill_id: Mapped[int] = mapped_column(Integer, ForeignKey("skills.id"), primary_key=True)
    usage_count: Mapped[int] = mapped_column(Integer, default=0)
    download_count: Mapped[int] = mapped_column(Integer, default=0)
    last_used: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
```

---

## 🔧 技术实现方案

### 核心集成方案：如何复用现有对话系统

本方案的核心思想是：**Skill内容就是System Prompt**，通过改变system prompt就能让AI扮演不同的角色。

#### 方案概述

```
┌─────────────────────────────────────────────────────────────┐
│  核心原理：Skill 的 SKILL.md 内容 = AI 的 System Prompt      │
└─────────────────────────────────────────────────────────────┘

【创建阶段】用户 → skill_builder → AI扮演"技能构建助手" → 生成新技能
                 ↓
            将 skill_builder 的 SKILL.md 作为 system prompt

【调试阶段】用户 → debug_session → AI扮演"被调试的技能" → 测试验证
                 ↓
            将被调试技能的 SKILL.md 作为 system prompt

【使用阶段】用户 → normal_session → AI扮演"已发布的技能" → 实际使用
                 ↓
            将已发布技能的 SKILL.md 作为 system prompt
```

#### 核心技术点

**1. Skill内容就是System Prompt**

```python
# 从 SkillDB 加载技能时
skill = await db_service.get_skill(skill_id)
system_prompt = skill.content  # SKILL.md 的内容

# 创建 ClaudeSDKClient 时使用
client = ClaudeSDKClient(
    options,
    system_prompt=system_prompt  # ← 关键：注入skill内容
)
```

**2. 调试时动态注入Skill**

```python
# 调试某个skill时
async def start_debug_session(skill_id: int, user_id: int):
    # 1. 从数据库加载要调试的skill
    skill = await db_service.get_skill(skill_id)

    # 2. 创建调试会话，session_type='debug'
    debug_session_id = f"debug_skill_{skill_id}_{user_id}_{timestamp}"

    # 3. 将skill的content作为system prompt注入
    await session_manager.create_session(
        session_id=debug_session_id,
        user_id=user_id,
        system_prompt=skill.content,  # ← 被调试skill的SKILL.md
        session_type='debug'
    )

    return debug_session_id
```

**3. 完全复用现有组件**

- ✅ **SkillLoader** - 加载skill的SKILL.md内容
- ✅ **AgentSession** - 管理会话（新增session_type字段）
- ✅ **query_in_session** - 多轮对话（无需修改）
- ✅ **ChatInterface** - 前端对话组件（无需修改）
- ✅ **SessionManager** - 会话管理（扩展支持session_type）

**4. 只需新增约20%的代码**

```
新增代码：
├── skill_builder/SKILL.md          # skill_builder提示词
├── skill_builder/skill.json        # skill_builder配置
├── backend/services/skill_debugger.py  # 调试服务
├── backend/api/v1/debug.py         # 调试API端点
├── frontend/components/SkillDebugger.tsx  # 调试界面（复用ChatInterface）
└── SessionDB.session_type          # 会话类型字段

复用代码（80%）：
├── SkillLoader, SkillManager       # 技能加载
├── AgentSession, SessionManager    # 会话管理
├── AgentService.query_in_session   # 对话逻辑
├── ChatInterface                   # 前端对话组件
└── SkillDB CRUD API                # 技能数据管理
```

#### 详细实现流程

**阶段1：Skill创建（使用skill_builder）**

```python
# 1. 用户点击"创建技能"
# 前端调用：
POST /api/skills/activate
Body: { "skill_name": "skill_builder" }

# 2. 后端激活skill_builder
@router.post("/skills/activate")
async def activate_skill(
    skill_name: str,
    current_user: UserDB = Depends(get_current_user)
):
    from services.skill_loader import SkillLoader

    # 加载skill_builder的SKILL.md
    loader = SkillLoader()
    skill_content = loader.load_skill_by_name("skill_builder")

    # 创建会话，将SKILL.md内容作为system prompt
    session_id = f"skill_builder_{current_user.id}_{timestamp}"
    system_prompt = skill_content['content']  # skill_builder的SKILL.md

    # 使用现有的query_in_session启动对话
    return {
        "session_id": session_id,
        "welcome_message": "你好！我是技能构建助手，让我们开始创建你的技能吧！"
    }

# 3. 用户与skill_builder对话（完全复用现有对话系统）
POST /api/chat
Body: {
    "session_id": "skill_builder_xxx",
    "message": "我想创建一个数据分析技能"
}

# skill_builder在对话中生成新skill的SKILL.md和skill.json
# 用户确认后保存到数据库
POST /api/platform/skills
Body: {
    "name": "data_analyzer",
    "content": "... SKILL.md内容 ...",
    "config": "... skill.json内容 ...",
    "status": "draft"
}
```

**阶段2：Skill调试（动态注入skill内容）**

```python
# 1. 用户点击"调试"按钮
POST /api/debug/start
Body: { "skill_id": 123 }

# 2. 后端创建调试会话
@router.post("/debug/start")
async def start_debug_session(
    skill_id: int,
    current_user: UserDB = Depends(get_current_user),
    session_manager: SessionManager = Depends(get_session_manager)
):
    # 从数据库加载要调试的skill
    skill = await db_service.get_skill(skill_id)

    # 创建调试会话，session_type='debug'
    debug_session_id = f"debug_skill_{skill_id}_{current_user.id}_{timestamp}"

    # 将skill的content作为system prompt注入
    await session_manager.create_session(
        session_id=debug_session_id,
        user_id=current_user.id,
        system_prompt=skill.content,  # ← 被调试skill的SKILL.md
        session_type='debug'
    )

    return {
        "debug_session_id": debug_session_id,
        "skill_name": skill.name
    }

# 3. 前端使用debug_session_id进行对话测试（完全复用ChatInterface）
<SkillDebugger>
  <ChatInterface sessionId={debugSessionId} />
</SkillDebugger>
```

**阶段3：Skill使用（已发布的skill）**

```python
# 1. 用户从技能市场启用某个skill
POST /api/skills/enable
Body: { "skill_id": 123 }

# 2. 在正常会话中使用该skill
# 系统自动将skill的content追加到system prompt
system_prompt = base_prompt + "\n\n" + skill.content

# AI现在会扮演这个skill的角色进行对话
```

#### 流程图

```
┌────────────────────────────────────────────────────────────────┐
│                      完整技能生命周期                            │
└────────────────────────────────────────────────────────────────┘

【创建阶段】
用户操作：点击"创建技能"
    ↓
后端处理：激活skill_builder
    ↓
关键实现：system_prompt = skill_builder的SKILL.md内容
    ↓
对话过程：用户与skill_builder多轮对话
    ↓
生成结果：新skill的SKILL.md + skill.json
    ↓
保存数据库：status='draft'

【调试阶段】
用户操作：点击"调试"按钮
    ↓
后端处理：创建debug会话
    ↓
关键实现：system_prompt = 被调试skill的SKILL.md内容
    ↓
特殊标记：session_type='debug'
    ↓
对话过程：用户测试skill功能
    ↓
调试结果：满意 → 发布 / 不满意 → 修改

【发布阶段】
用户操作：点击"发布到市场"
    ↓
后端处理：更新status='published'
    ↓
市场展示：其他用户可见并可用

【使用阶段】
用户操作：从市场启用skill
    ↓
后端处理：在会话中注入skill内容
    ↓
关键实现：system_prompt += 启用的skill的SKILL.md
    ↓
对话过程：AI扮演该skill角色
```

#### 关键优势

✅ **最小化代码变更**：只需新增约20%代码
✅ **完全复用**：对话系统、会话管理、前端组件无需修改
✅ **一致体验**：创建、调试、使用都使用相同的对话界面
✅ **易于维护**：skill内容就是系统提示词，逻辑清晰
✅ **灵活扩展**：可以轻松支持skill组合、优先级等高级特性

---

### 实现1：Meta Skill 实现

#### 1.1 skill_builder 技能文件

**目录结构**：
```
backend/.claude/skills/skill_builder/
├── SKILL.md          # 技能内容
└── skill.json        # 技能配置
```

**SKILL.md 内容**：
```markdown
---
name: skill_builder
description: 技能构建助手，通过对话帮助用户创建自定义AI技能
category: 技能开发
tags: ["技能创建", "对话式构建", "技能生成"]
---

# Skill Builder - 技能构建助手

你是专业的AI技能构建助手，帮助用户通过对话创建高质量的AI技能。

## 核心能力

1. **需求收集** - 通过多轮对话理解用户需求
2. **技能设计** - 设计技能结构和配置
3. **内容生成** - 生成 SKILL.md 和 skill.json
4. **测试验证** - 在对话中测试技能效果
5. **发布指导** - 引导用户发布技能

## 工作流程

### 阶段1：需求理解

🤔 **请描述你想要创建的技能：**

收集以下信息：
- **技能名称**：简洁、描述性的名称
- **主要功能**：技能的核心功能是什么？
- **使用场景**：什么时候会用到这个技能？
- **目标用户**：谁会使用这个技能？
- **特殊要求**：有什么特殊的需求或限制？

### 阶段2：技能设计

📋 **我将为你设计技能结构：**

基于用户需求，设计：
- **技能分类**：确定技能所属类别
- **能力边界**：定义技能的能力范围
- **工具需求**：需要使用哪些工具？
- **配置参数**：skill.json 配置项

### 阶段3：内容生成

✨ **生成技能内容：**

- **SKILL.md**：
  - Frontmatter 元数据
  - 技能描述
  - 使用指南
  - 示例代码

- **skill.json**：
  - 基本信息
  - 版本号
  - 依赖声明
  - 配置参数

### 阶段4：测试验证

🧪 **让我们测试这个技能：**

提供测试用例：
- 典型使用场景
- 边界情况测试
- 错误处理测试

根据测试结果优化技能内容。

### 阶段5：发布准备

🚀 **准备发布到技能市场：**

确认信息：
- 技能名称: {name}
- 描述: {description}
- 分类: {category}
- 是否公开: {is_public}

用户确认后，调用 API 保存技能。

## 交互模板

### 初始化对话

```
你好！我是技能构建助手 🛠️

我可以帮你创建自定义的 AI 技能。

你想创建什么样的技能？请告诉我：
1. 技能的主要功能
2. 使用场景
3. 任何特殊要求
```

### 需求确认

```
🤔 让我确认一下你的需求：

**技能名称**：{name}
**主要功能**：{function}
**使用场景**：{scenario}
**特殊要求**：{requirements}

这样理解对吗？如果需要调整，请告诉我。
```

### 技能设计展示

```
📋 我已为你设计好技能结构：

## skill.json
\`\`\`json
{skill_json_content}
\`\`\`

## SKILL.md 大纲
\`\`\`markdown
{skill_md_outline}
\`\`\`

你想要：
1. 继续生成完整内容
2. 修改某些部分
3. 重新设计

请选择或提出修改意见。
```

### 测试结果分析

```
🧪 测试结果：

**测试用例**：{test_case}
**执行结果**：{result}
**分析**：{analysis}

**建议优化**：
- {optimization_1}
- {optimization_2}

是否需要调整技能内容？
```

### 发布确认

```
🚀 准备发布！

**技能信息**：
- 名称: {name}
- 描述: {description}
- 分类: {category}
- 初始状态: 草稿（仅你可见）

发布后，你可以：
- 在对话中继续调试
- 满意后发布到市场
- 设置为公开或私有

确认创建？[确认] [修改]
```

## 技能模板库

### 模板1：数据分析技能

适用于：
- 数据可视化
- 统计分析
- 报表生成

### 模板2：文本处理技能

适用于：
- 文本审校
- 格式转换
- 内容生成

### 模板3：工具调用技能

适用于：
- API 调用
- 文件操作
- 外部服务集成

### 模板4：指导框架技能

适用于：
- 任务规划
- 流程指导
- 最佳实践

## 最佳实践

### 技能命名
- ✅ 简洁明了（2-4个词）
- ✅ 描述性强
- ✅ 使用动词或名词
- ❌ 避免过于技术化

### 技能描述
- ✅ 一句话说明核心功能
- ✅ 包含使用场景
- ✅ 突出独特价值

### SKILL.md 编写
- ✅ 结构清晰
- ✅ 示例丰富
- ✅ 易于理解
- ✅ 包含边界说明

## 常见问题

**Q: 创建技能需要编程知识吗？**
A: 不需要！我会通过对话引导你，自动生成所有代码。

**Q: 技能创建后可以修改吗？**
A: 可以！你可以随时编辑、调试和优化技能。

**Q: 如何发布技能到市场？**
A: 调试满意后，点击"发布到市场"按钮即可。

**Q: 技能可以被其他人使用吗？**
A: 发布时选择"公开"，其他用户就可以在市场中找到并使用你的技能。

---

开始创建你的第一个技能吧！🎉
```

#### 1.2 skill.json 配置

```json
{
  "name": "skill_builder",
  "version": "1.0.0",
  "description": "技能构建助手，通过对话帮助用户创建自定义AI技能",
  "author": "System",
  "category": "技能开发",
  "tags": ["技能创建", "对话式构建", "技能生成", "低代码"],
  "capabilities": [
    "requirement_collection",
    "skill_design",
    "content_generation",
    "testing_guidance",
    "publishing_assistance"
  ],
  "requirements": {
    "dependencies": []
  },
  "configuration": {
    "default_config": {
      "enable_templates": true,
      "enable_testing": true,
      "auto_format": true
    }
  }
}
```

---

### 实现2：技能构建API

#### 2.1 API 端点设计

```python
# backend/api/v1/platform.py（扩展）

@router.post("/skills/build", response_model=SkillResponse)
async def build_skill_from_conversation(
    request: SkillBuildRequest,
    current_user: UserDB = Depends(get_current_user),
    config_manager: ConfigurationManager = Depends(get_configuration_manager)
):
    """
    从对话中构建技能

    Args:
        request: 包含对话上下文的请求
        current_user: 当前用户

    Returns:
        SkillResponse: 创建的技能
    """
    try:
        # 调用技能构建服务
        skill_builder = SkillBuilderService(config_manager.db_service)

        skill = await skill_builder.build_skill_from_conversation(
            conversation_context=request.conversation_context,
            user_id=current_user.id
        )

        return SkillResponse.from_orm(skill)

    except Exception as e:
        logger.error(f"Error building skill: {e}")
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=str(e)
        )

@router.post("/skills/{skill_id}/debug")
async def start_debug_session(
    skill_id: int,
    current_user: UserDB = Depends(get_current_user)
):
    """
    启动技能调试会话

    Args:
        skill_id: 技能ID
        current_user: 当前用户

    Returns:
        调试会话ID
    """
    try:
        debugger = SkillDebugger()
        session_id = await debugger.start_debug_session(skill_id, current_user.id)

        return {
            "session_id": session_id,
            "skill_id": skill_id,
            "message": "调试会话已启动"
        }

    except Exception as e:
        logger.error(f"Error starting debug session: {e}")
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=str(e)
        )

@router.post("/skills/{skill_id}/publish")
async def publish_skill(
    skill_id: int,
    publish_request: SkillPublishRequest,
    current_user: UserDB = Depends(get_current_user)
):
    """
    发布技能到市场

    Args:
        skill_id: 技能ID
        publish_request: 发布请求（包含是否公开等信息）
        current_user: 当前用户

    Returns:
        更新后的技能
    """
    try:
        async with config_manager.db_service.async_session() as session:
            from sqlalchemy import select

            # 获取技能
            stmt = select(SkillDB).where(
                SkillDB.id == skill_id,
                SkillDB.created_by == current_user.id  # 只能发布自己的技能
            )
            result = await session.execute(stmt)
            skill = result.scalar_one_or_none()

            if not skill:
                raise HTTPException(
                    status_code=status.HTTP_404_NOT_FOUND,
                    detail="Skill not found"
                )

            # 更新发布状态
            skill.is_public = publish_request.is_public
            skill.updated_at = datetime.utcnow()

            await session.commit()
            await session.refresh(skill)

            return SkillResponse.from_orm(skill)

    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"Error publishing skill: {e}")
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=str(e)
        )
```

#### 2.2 请求/响应模型

```python
# backend/models/platform.py（扩展）

class SkillBuildRequest(BaseModel):
    """技能构建请求"""
    conversation_context: Dict[str, Any]
    skill_metadata: Optional[Dict[str, Any]] = None

class SkillPublishRequest(BaseModel):
    """技能发布请求"""
    is_public: bool = True
    category: Optional[str] = None
    tags: Optional[List[str]] = None
```

---

### 实现3：数据库扩展

#### 3.1 新增表结构

```sql
-- 技能评分表
CREATE TABLE skill_ratings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    skill_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (skill_id) REFERENCES skills(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    UNIQUE(skill_id, user_id)  -- 每个用户对每个技能只能评分一次
);

-- 技能使用统计表
CREATE TABLE skill_usage_stats (
    skill_id INTEGER PRIMARY KEY,
    usage_count INTEGER DEFAULT 0,
    download_count INTEGER DEFAULT 0,
    favorite_count INTEGER DEFAULT 0,
    last_used TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (skill_id) REFERENCES skills(id) ON DELETE CASCADE
);

-- 技能评论表
CREATE TABLE skill_comments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    skill_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    comment TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (skill_id) REFERENCES skills(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

---

## 📊 用户使用流程

### 流程1：创建技能

```
用户进入技能市场
    ↓
点击"创建技能"按钮
    ↓
选择：
    1. 从模板创建
    2. 从零开始
    ↓
启动 skill_builder 对话
    ↓
多轮对话收集需求：
    - 想创建什么技能？
    - 主要功能是什么？
    - 有什么特殊要求？
    ↓
AI 生成 skill.json 和 SKILL.md
    ↓
用户确认或修改
    ↓
保存为草稿（is_public=False）
```

### 流程2：调试技能

```
用户进入"我的技能"
    ↓
选择要调试的技能
    ↓
点击"调试"按钮
    ↓
打开调试界面（分屏）
    左侧：调试对话
    右侧：技能内容预览
    ↓
在调试对话中测试技能
    ↓
实时查看响应
    ↓
根据反馈修改技能
    ↓
保存修改
```

### 流程3：发布技能

```
用户进入"我的技能"
    ↓
选择要发布的技能
    ↓
点击"发布到市场"按钮
    ↓
填写发布信息：
    - 技能名称
    - 描述
    - 分类
    - 标签
    - 是否公开
    ↓
确认发布
    ↓
技能状态更新为已发布（is_public=True）
    ↓
出现在技能市场中
```

### 流程4：使用技能

```
用户浏览技能市场
    ↓
找到感兴趣的技能
    ↓
查看技能详情
    ↓
点击"启用"或"试用"
    ↓
技能加载到当前会话
    ↓
在对话中使用该技能
    ↓
系统记录使用统计（usage_count++）
```

---

## ✅ 现有基础能力验证

### 已具备的能力（80%）

#### 1. 技能管理系统 ✅

**位置**：
- `backend/models/database.py` - SkillDB 模型
- `backend/api/v1/platform.py` - 技能 CRUD API
- `backend/services/skill_loader.py` - 技能加载器
- `backend/services/skills/skill_manager.py` - 技能管理器

**功能**：
- ✅ 技能增删改查
- ✅ 技能配置（skill.json）
- ✅ 技能内容（SKILL.md）
- ✅ 分类和标签
- ✅ 公开/私有控制

#### 2. 对话系统 ✅

**位置**：
- `backend/services/session_manager.py` - 会话管理
- `backend/services/agent_service.py` - 对话服务
- `backend/models/database.py` - MessageDB 模型

**功能**：
- ✅ 多轮对话
- ✅ 流式响应
- ✅ 会话持久化
- ✅ 消息历史
- ✅ 工具调用追踪

#### 3. 前端组件 ✅

**位置**：
- `frontend/aigc-frontend/components/ChatInterface.tsx` - 对话界面
- `frontend/aigc-frontend/components/SkillMarket.tsx` - 技能市场
- `frontend/aigc-frontend/components/SkillGalaxy.tsx` - 技能银河
- `frontend/aigc-frontend/services/platformService.ts` - 平台服务

**功能**：
- ✅ 对话界面
- ✅ 技能展示
- ✅ 技能选择
- ✅ API 调用封装

### 需要新增的能力（20%）

#### 1. Meta Skill（skill_builder）

**新增文件**：
- `backend/.claude/skills/skill_builder/SKILL.md`
- `backend/.claude/skills/skill_builder/skill.json`

**核心功能**：
- 对话式需求收集
- 技能内容生成
- 测试指导
- 发布引导

#### 2. 调试功能

**新增文件**：
- `backend/services/skill_debugger.py` - 调试服务

**核心功能**：
- 调试会话管理
- 技能临时加载
- 测试用例执行

#### 3. 市场功能

**新增文件**：
- `frontend/aigc-frontend/pages/skill-market.tsx` - 市场页面
- `frontend/aigc-frontend/components/SkillCard.tsx` - 技能卡片
- `frontend/aigc-frontend/components/SkillDetail.tsx` - 技能详情

**核心功能**：
- 技能列表
- 分类筛选
- 搜索功能
- 评分展示

---

## 🎯 开发优势

### 1. 复用度高

- ✅ 现有技能管理系统完整可用
- ✅ 对话系统成熟稳定
- ✅ 前端组件丰富
- ✅ 数据库模型完善

### 2. 开发周期短

- ✅ 核心功能只需新增 20% 代码
- ✅ 无需大规模重构
- ✅ 渐进式开发
- ✅ 风险可控

### 3. 用户体验好

- ✅ 对话式创建，无需学习编程
- ✅ 实时调试，即时反馈
- ✅ 模板丰富，降低门槛
- ✅ 一键发布，简单快捷

### 4. 可扩展性强

- ✅ 支持技能模板库
- ✅ 支持评分和评论
- ✅ 支持推荐算法
- ✅ 支持审核机制

---

## 📊 下一步行动

### 阶段1：核心构建功能（1-2周）

1. **创建 skill_builder 技能**
   - 编写 SKILL.md
   - 编写 skill.json
   - 测试基础对话流程

2. **实现构建API**
   - build_skill_from_conversation
   - 技能生成逻辑
   - 数据库保存

3. **前端构建界面**
   - 创建技能页面
   - 集成对话组件
   - 表单确认

### 阶段2：调试功能（1周）

1. **实现调试服务**
   - SkillDebugger 类
   - 调试会话管理
   - 技能临时加载

2. **前端调试界面**
   - 分屏布局
   - 测试对话
   - 实时预览

### 阶段3：市场功能（1周）

1. **市场页面**
   - 技能列表
   - 分类筛选
   - 搜索功能

2. **技能详情**
   - 详情展示
   - 评分系统
   - 启用按钮

3. **评分统计**
   - 数据库表
   - API接口
   - 前端展示

---

**最后更新**: 2026-01-14
**当前状态**: 可行性验证完成，准备开始开发
